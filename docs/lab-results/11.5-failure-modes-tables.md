# Step 11.5 — failure-mode behaviour, Go vs Rust

Host `lab-x86-3`, netns lab, config **S1**, 18 runs.

- go client: host x86_64, go linux/amd64 (none), profile reference, rev e55a8dc, deployed 2026-09-24T21:07:13Z
- go server: host x86_64, go linux/amd64 (none), profile reference, rev e55a8dc, deployed 2026-09-24T21:07:13Z
- rust client: host x86_64, rust x86_64-unknown-linux-gnu (glibc 2.17), profile release, rev e55a8dc, deployed 2026-09-24T21:07:13Z
- rust server: host x86_64, rust x86_64-unknown-linux-gnu (glibc 2.17), profile release, rev e55a8dc, deployed 2026-09-24T21:07:13Z

## Server restarted (SIGTERM) during traffic

*Server restart during traffic: new streams work, old streams fail cleanly.*

Timeline: t+20s stop-server TERM, t+24s start-server.

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | 35.9 | 40.0 | 40.0 | 593 | 2.1 | 1 | 1 | 0 | 2 | 3 | PASS |
| RR | 36.0 | 41.0 | 41.0 | 589 | 1.5 | 1 | 1 | 0 | 2 | 3 | PASS |

- `GG` recovery_from_heal_s = 35.9 (want 20–110) ok — smux keepalive (30 s interval, 30 s timeout) needs one dead tick and then one more; 09.3 measured 64.89 s
- `GG` post_heal_requests = 490 (want ≥ 100) ok — traffic really resumed, not one lucky exchange
- `GG` post_heal_errors = 0 (want 0–3) ok — once a session is re-dialled the path is healthy again
- `GG` log[cli] 'smux version: 2 on connection:' = 2 (want ≥ 2) ok — the recovery mechanism, in the client's own words: a SECOND session is dialled (client/main.go:473)
- `GG` log[cli] 're-connecting:' = 0 (want ≤ 0) ok — the plan's bullet named this and 09.3 corrected it: that loop runs only when `createConn()` fails, and dialling UDP does not fail — so the line a restart is supposed to produce must not be there
- `RR` recovery_from_heal_s = 36.0 (want 20–110) ok — smux keepalive (30 s interval, 30 s timeout) needs one dead tick and then one more; 09.3 measured 64.89 s
- `RR` post_heal_requests = 492 (want ≥ 100) ok — traffic really resumed, not one lucky exchange
- `RR` post_heal_errors = 0 (want 0–3) ok — once a session is re-dialled the path is healthy again
- `RR` log[cli] 'smux version: 2 on connection:' = 2 (want ≥ 2) ok — the recovery mechanism, in the client's own words: a SECOND session is dialled (client/main.go:473)
- `RR` log[cli] 're-connecting:' = 0 (want ≤ 0) ok — the plan's bullet named this and 09.3 corrected it: that loop runs only when `createConn()` fails, and dialling UDP does not fail — so the line a restart is supposed to produce must not be there

## Server SIGKILLed during traffic

*SIGKILL of either end (the server half).*

Timeline: t+20s stop-server KILL, t+24s start-server.

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | 36.0 | 41.0 | 40.0 | 591 | 2.1 | 1 | 1 | 0 | 2 | 3 | PASS |
| RR | 36.7 | 42.0 | 41.0 | 588 | 1.4 | 1 | 1 | 0 | 2 | 3 | PASS |

- `GG` recovery_from_heal_s = 36.0 (want 20–110) ok — a killed server is indistinguishable from a blackholed one, so the same keepalive path recovers it
- `GG` post_heal_requests = 490 (want ≥ 100) ok
- `GG` log[cli] 'smux version: 2 on connection:' = 2 (want ≥ 2) ok — a killed server is recovered from by the same re-dial
- `RR` recovery_from_heal_s = 36.7 (want 20–110) ok — a killed server is indistinguishable from a blackholed one, so the same keepalive path recovers it
- `RR` post_heal_requests = 492 (want ≥ 100) ok
- `RR` log[cli] 'smux version: 2 on connection:' = 2 (want ≥ 2) ok — a killed server is recovered from by the same re-dial

## Client SIGKILLed and restarted

*SIGKILL of either end (the client half); the server replaces the session.*

Timeline: t+20s stop-client KILL, t+24s start-client.

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | 1.9 | 5.0 | 5.0 | 420 | 2.0 | 18 | 1 | 0 | 1 | 2 | PASS |
| RR | 1.1 | 5.0 | 5.0 | 423 | 1.5 | 17 | 1 | 0 | 1 | 2 | PASS |

- `GG` recovery_from_heal_s = 1.9 (want 0–20) ok — the workload reconnects to the new listener at once; nothing has to time out, because the client dials a fresh conv
- `GG` post_heal_requests = 319 (want ≥ 100) ok
- `RR` recovery_from_heal_s = 1.1 (want 0–20) ok — the workload reconnects to the new listener at once; nothing has to time out, because the client dials a fresh conv
- `RR` post_heal_requests = 320 (want ≥ 100) ok

## Path blackholed for 45 s (netem loss 100%)

*Blackhole the path: smux keepalive closes the session; the client re-dials when traffic resumes.*

Timeline: t+20s netem blackhole, t+65s netem clean.

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | 1.9 | 48.0 | 48.0 | 553 | 2.1 | 1 | 1 | 0 | 2 | 2 | PASS |
| RR | 1.6 | 47.0 | 47.0 | 558 | 1.5 | 1 | 1 | 0 | 2 | 2 | PASS |

- `GG` outage_s = 48.0 (want ≥ 40) ok — the 45 s blackhole must actually stop traffic
- `GG` recovery_from_heal_s = 1.9 (want 0–60) ok — once packets flow again the session is either alive or re-dialled
- `GG` post_heal_requests = 454 (want ≥ 100) ok
- `RR` outage_s = 47.0 (want ≥ 40) ok — the 45 s blackhole must actually stop traffic
- `RR` recovery_from_heal_s = 1.6 (want 0–60) ok — once packets flow again the session is either alive or re-dialled
- `RR` post_heal_requests = 458 (want ≥ 100) ok

## Session rotation under load (-autoexpire 30 -scavengettl 15)

*autoexpire/scavengettl under load: rotation without dropped new streams, old sessions closed after the TTL.*

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | — | — | 1.0 | 732 | 2.1 | 3 | 3 | 0 | 4 | 4 | PASS |
| RR | — | — | 1.0 | 735 | 1.4 | 3 | 3 | 0 | 4 | 4 | PASS |

- `GG` reconnects = 3 (want ≥ 1) ok — the scavenger closes the expired session, which ends the stream on it — kcptun rotates by closing, there is no stream migration
- `GG` max_gap_s = 1.0 (want 0–15) ok — a rotation costs one reconnect, not an outage
- `GG` requests = 732 (want ≥ 300) ok
- `GG` client_active_opens = 4 (want ≥ 2) ok — a floor on dials, not evidence of rotation: under `conn 4` the second accepted connection dials a second session on its own (reference/kcptun/client/main.go:429-437 dials whenever `muxes[idx].session == nil`). The discriminator is the TTL log check below
- `GG` log[cli] 'scavenger: session closed due to ttl:' = 3 (want ≥ 1) ok — reference/kcptun/client/main.go:587 — the TTL path, not the normally-closed one. THIS is what says a rotation happened
- `RR` reconnects = 3 (want ≥ 1) ok — the scavenger closes the expired session, which ends the stream on it — kcptun rotates by closing, there is no stream migration
- `RR` max_gap_s = 1.0 (want 0–15) ok — a rotation costs one reconnect, not an outage
- `RR` requests = 735 (want ≥ 300) ok
- `RR` client_active_opens = 4 (want ≥ 2) ok — a floor on dials, not evidence of rotation: under `conn 4` the second accepted connection dials a second session on its own (reference/kcptun/client/main.go:429-437 dials whenever `muxes[idx].session == nil`). The discriminator is the TTL log check below
- `RR` log[cli] 'scavenger: session closed due to ttl:' = 3 (want ≥ 1) ok — reference/kcptun/client/main.go:587 — the TTL path, not the normally-closed one. THIS is what says a rotation happened

## Port-range server, client hopping across sessions

*A port range server with the client hopping across sessions.*

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | ports dialled | out of range | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | — | — | 1.0 | 733 | 2.1 | 3 | 3 | 0 | 4 | 4 | 2 | 0 | PASS |
| RR | — | — | 1.0 | 734 | 1.5 | 3 | 3 | 0 | 4 | 4 | 2 | 0 | PASS |

- `GG` reconnects = 3 (want ≥ 1) ok
- `GG` max_gap_s = 1.0 (want 0–15) ok — hopping to another port of the range is not an outage
- `GG` requests = 733 (want ≥ 300) ok
- `GG` client_active_opens = 4 (want ≥ 2) ok — a floor on dials, not evidence of rotation: under `conn 4` the second accepted connection dials a second session on its own (reference/kcptun/client/main.go:429-437). The discriminator is the TTL log check below
- `GG` remote_ports_out_of_range = 0 (want 0–0) ok — every dial must land inside 29904-29907 (reference/kcptun/client/dial.go:56)
- `GG` log[cli] 'scavenger: session closed due to ttl:' = 3 (want ≥ 1) ok — reference/kcptun/client/main.go:587 — the rotation this case hops across the range with
- `RR` reconnects = 3 (want ≥ 1) ok
- `RR` max_gap_s = 1.0 (want 0–15) ok — hopping to another port of the range is not an outage
- `RR` requests = 734 (want ≥ 300) ok
- `RR` client_active_opens = 4 (want ≥ 2) ok — a floor on dials, not evidence of rotation: under `conn 4` the second accepted connection dials a second session on its own (reference/kcptun/client/main.go:429-437). The discriminator is the TTL log check below
- `RR` remote_ports_out_of_range = 0 (want 0–0) ok — every dial must land inside 29904-29907 (reference/kcptun/client/dial.go:56)
- `RR` log[cli] 'scavenger: session closed due to ttl:' = 3 (want ≥ 1) ok — reference/kcptun/client/main.go:587 — the rotation this case hops across the range with

## Server restarted, `--tcp` (fake TCP) transport

*--tcp variants of restart and blackhole (the restart half).*

Timeline: t+20s stop-server TERM, t+24s start-server.

Runs only as RR, RG: the Go `--tcp` client carries no data (DECISIONS V22), so a Go-client arm would measure that and not the restart

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RR | 36.1 | 40.0 | 40.0 | 594 | 1.5 | 1 | 1 | 0 | 2 | 1 | PASS |

- `RR` recovery_from_heal_s = 36.1 (want 0–120) ok — fake TCP is still KCP above it, so the same keepalive path recovers it
- `RR` post_heal_requests = 491 (want ≥ 50) ok

## Path blackholed for 45 s, `--tcp` transport

*--tcp variants of restart and blackhole (the blackhole half).*

Timeline: t+20s netem blackhole, t+65s netem clean.

Runs only as RR, RG: the Go `--tcp` client carries no data (DECISIONS V22)

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RR | 1.9 | 47.0 | 47.0 | 560 | 1.5 | 1 | 1 | 2 | 2 | 2 | PASS |

- `RR` outage_s = 47.0 (want ≥ 40) ok
- `RR` recovery_from_heal_s = 1.9 (want 0–90) ok
- `RR` post_heal_requests = 458 (want ≥ 50) ok
- `RR` log[cli] 're-connecting: dial(): tcpraw.Dial():' = 2 (want ≥ 1) ok — fake TCP is the one transport whose `createConn()` can fail, and the wrap chain is Go's own (client/dial.go:67, client/main.go:505)

## Target refuses the connection

*Target unreachable: the stream closes and the client sees EOF.*

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | — | — | — | 0 | — | 176 | 176 | 0 | 4 | 4 | PASS |
| RR | — | — | — | 0 | — | 178 | 178 | 0 | 4 | 4 | PASS |

- `GG` requests = 0 (want 0–0) ok — nothing can be echoed: there is no target
- `GG` errors = 176 (want ≥ 20) ok — every attempt fails, and fails fast — an RST is not a timeout
- `GG` median_error_interval_s = 0.0 (want 0–2) ok — a refused dial returns immediately; the 250 ms reconnect pause is the only delay
- `RR` requests = 0 (want 0–0) ok — nothing can be echoed: there is no target
- `RR` errors = 178 (want ≥ 20) ok — every attempt fails, and fails fast — an RST is not a timeout
- `RR` median_error_interval_s = 0.0 (want 0–2) ok — a refused dial returns immediately; the 250 ms reconnect pause is the only delay

## Target address blackholed (Go's 10 s dialTimeout)

*Target unreachable: dial fails after 10 s and the stream closes.*

| pair | recovery s | outage s | max gap s | requests | p50 ms | errors | reconn | re-connecting | cli ActiveOpens | srv PassiveOpens | verdict |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GG | — | — | — | 0 | — | 7 | 7 | 0 | 4 | 4 | PASS |
| RR | — | — | — | 0 | — | 7 | 7 | 0 | 4 | 4 | PASS |

- `GG` requests = 0 (want 0–0) ok
- `GG` errors = 7 (want ≥ 2) ok
- `GG` median_error_interval_s = 10.0 (want 8–14) ok — reference/kcptun/server/main.go:488 `const dialTimeout = 10 * time.Second`, plus the workload's 250 ms pause
- `RR` requests = 0 (want 0–0) ok
- `RR` errors = 7 (want ≥ 2) ok
- `RR` median_error_interval_s = 10.0 (want 8–14) ok — reference/kcptun/server/main.go:488 `const dialTimeout = 10 * time.Second`, plus the workload's 250 ms pause

## Verdict

18 of 18 runs met every check.

